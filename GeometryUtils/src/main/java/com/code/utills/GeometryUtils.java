package com.code.utills;

import com.code.geometry.AbstractShape;
import com.code.geometry.Circle;

public class GeometryUtils {
    public static void showGeometryType(AbstractShape shape){
        shape.draw();
    }
}
